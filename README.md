# Audio Ses Analizi

pyaudio kütüphanesi python 3.12.0 kullanılmaktadır.
Kullanılan kütüphaneler;
`pip install pyaudio`
`pip install librosa`
`pip install sequential`
`pip install numpy`
`pip install keras`
